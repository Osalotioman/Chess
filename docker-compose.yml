services:
  ws-server:
    build:
      context: ./ws-server
      dockerfile: Dockerfile
    ports:
      - "${WS_PORT:-8081}:8081"
    environment:
      HOST: 0.0.0.0
      PORT: 8081
    restart: unless-stopped

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "${CLIENT_PORT:-3001}:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
    depends_on:
      - ws-server
    restart: unless-stopped
